# Общие зависимости для DEV и PROD.
# Версии намеренно не зафиксированы (см. план: "latest релиз на текущий
# момент") — при подготовке к продакшену рекомендуется зафиксировать их
# через `pip freeze > requirements/lock.txt`.

Django
psycopg[binary]
djangorestframework
djangorestframework-simplejwt
channels
channels-redis
# ASGI-сервер: нужен и в dev (uvicorn напрямую, вместо runserver — у channels
# 4.x нет ASGI-варианта runserver), и в prod (gunicorn -k UvicornWorker)
uvicorn[standard]
django-cors-headers
django-health-check
django-prometheus
celery[redis]
geoip2
colorlog
django-treebeard>=7.0.1
django-taggit
Pillow
django-ckeditor-5
nh3
django-imagekit
pillow-heif
django-tables2
django-filter
django-htmx
Markdown
