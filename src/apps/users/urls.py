"""
URL-маршруты приложения users.

Пока пусто — это заготовка (см. Задачу 1: apps/users создан только как
структура). Реальные эндпоинты (регистрация, логин, JWT refresh, профиль
и т.д.) добавляются отдельным этапом.
"""

from django.urls import path

app_name = "users"

urlpatterns = [
    # path("register/", views.RegisterView.as_view(), name="register"),
]
